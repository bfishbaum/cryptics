# Environment variables for local development
NODE_ENV=development
PORT=3001

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cryptics
DB_USER=postgres
DB_PASSWORD=cryptics_password

# For Docker development, use these values:
# DB_HOST=postgres (when running with docker-compose)
# DB_PASSWORD=cryptics_password